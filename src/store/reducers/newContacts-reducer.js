
const initialState = {
    newContacts: {}
};

const newContactsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default newContactsReducer;